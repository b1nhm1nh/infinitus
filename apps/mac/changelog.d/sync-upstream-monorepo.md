Mac: the fork sync brings upstream's apps/mac layout, swapd engine and retired-surface cleanups, keeping the 9Router env-routing fleet lead, the formatter-free day keys and the pop-out height clamps.
