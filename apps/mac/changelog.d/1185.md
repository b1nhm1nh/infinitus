Desktop: A thread that fails now raises a notification, like one waiting for input.
Desktop: While the window has focus, an alert arrives as an in-app toast instead of a system banner, and the thread you are reading stays silent.
Desktop: Starting a thread in a new worktree shows each setup step — fetch, checkout, submodules, setup script — and can be cancelled while it runs.
Desktop: A thread whose project is not a git repository, or whose base branch has no commit, now runs in the project checkout instead of failing.
